import { ChevronLeft, ChevronRight } from "lucide-react";
import { formatDate } from "../../../utils/formatters";

export default function PeriodNavigator({ start, end, isCurrent, onPrevious, onNext, onCurrent }: { start: Date; end: Date; isCurrent: boolean; onPrevious: () => void; onNext: () => void; onCurrent: () => void }) {
  return <div className="flex flex-wrap items-center justify-center gap-2 rounded-xl border border-slate-200 bg-white p-3 shadow-sm"><button type="button" aria-label="Poprzedni okres" className="inline-flex h-9 w-9 items-center justify-center rounded-lg border border-slate-300 text-slate-700 hover:bg-slate-100" onClick={onPrevious}><ChevronLeft size={18} aria-hidden="true" /></button><strong className="min-w-[220px] text-center text-sm text-slate-900">{formatDate(start)} – {formatDate(end)}</strong><button type="button" aria-label="Następny okres" disabled={isCurrent} className="inline-flex h-9 w-9 items-center justify-center rounded-lg border border-slate-300 text-slate-700 hover:bg-slate-100 disabled:cursor-not-allowed disabled:opacity-40" onClick={onNext}><ChevronRight size={18} aria-hidden="true" /></button><button type="button" disabled={isCurrent} className="ml-1 rounded-lg border border-blue-200 bg-blue-50 px-3 py-2 text-sm font-semibold text-blue-700 hover:bg-blue-100 disabled:cursor-default disabled:opacity-60" onClick={onCurrent}>Bieżący</button></div>;
}
