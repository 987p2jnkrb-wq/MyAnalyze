{
  "name": "myanalyze-backend",
  "version": "1.3.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test:api": "npm run build && node tests/mutation-queue.mjs && node tests/startup-maintenance.mjs && node tests/api-smoke.mjs"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
